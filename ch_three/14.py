# 14. ☼ Create a variable words containing a list of words. 
# Experiment with words.sort() and sorted(words). What is the difference?

# the dot notation actually changes the list itself. calling it as an argument doesn't
# change the variable. is that how dot notation generally work?